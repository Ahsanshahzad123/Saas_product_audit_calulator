<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- ═══════════════════════════════════════════════════════════════════════
       EMBEDDABLE CALCULATOR  (no layout wrapper — used by page + snippet)
       ═══════════════════════════════════════════════════════════════════════ -->
  <template id="calculator_embed" name="SaaS Calculator Embed">

    <div class="saas-calc-root">

      <section class="sc-header">
        <h1>Free SaaS Opportunity Audit</h1>
        <p>Answer 7 quick questions and find out exactly how much revenue you're leaving on the table — and what it costs to build it right.</p>
      </section>

      <div class="sc-container">

        <!-- ── FORM ────────────────────────────────────────────────── -->
        <div class="sc-form-panel">

          <div class="sc-field">
            <label>Which best describes your situation?</label>
            <select id="fAudience">
              <option value="idea_personal">I have an idea + personal investment</option>
              <option value="idea_funded">I have an idea + seed / investor funding</option>
              <option value="unsatisfied_team">I have a dev team but unhappy with results</option>
              <option value="mvp_investment">I have an MVP + investment, need a better team</option>
              <option value="mvp_ai">Built MVP with AI tools — facing technical issues</option>
              <option value="non_technical">Non-technical founder with limited budget</option>
            </select>
          </div>

          <div class="sc-row">
            <div class="sc-field">
              <label>Type of SaaS Product</label>
              <select id="fSaasType">
                <option value="b2b">B2B SaaS (selling to businesses)</option>
                <option value="b2c">B2C / Consumer SaaS</option>
                <option value="marketplace">Marketplace / Platform</option>
                <option value="enterprise">Enterprise Software</option>
              </select>
            </div>
            <div class="sc-field">
              <label>Product Complexity</label>
              <select id="fComplexity">
                <option value="mvp">Simple MVP (core features only)</option>
                <option value="standard" selected="">Standard SaaS (full feature set)</option>
                <option value="complex">Complex Platform (APIs, integrations)</option>
                <option value="enterprise">Enterprise-grade (compliance, SSO)</option>
              </select>
            </div>
          </div>

          <div class="sc-row">
            <div class="sc-field">
              <label>Target Users in Year 1</label>
              <select id="fUsers">
                <option value="small">Less than 500 users</option>
                <option value="medium" selected="">500 – 5,000 users</option>
                <option value="large">5,000 – 50,000 users</option>
                <option value="xlarge">50,000+ users</option>
              </select>
            </div>
            <div class="sc-field">
              <label>When Do You Need to Launch?</label>
              <select id="fTimeline">
                <option value="asap">ASAP — within 3 months</option>
                <option value="short" selected="">3 – 6 months</option>
                <option value="medium">6 – 12 months</option>
                <option value="long">12+ months (no rush)</option>
              </select>
            </div>
          </div>

          <div class="sc-row">
            <div class="sc-field">
              <label>AI / Technology Component</label>
              <select id="fAI">
                <option value="no_ai">No AI features</option>
                <option value="ai_api">AI via API (OpenAI, Claude…)</option>
                <option value="ai_custom">Custom ML model</option>
                <option value="ai_core">AI-powered core product</option>
              </select>
            </div>
            <div class="sc-field">
              <label>Industry / Compliance</label>
              <select id="fIndustry">
                <option value="general">General / Other</option>
                <option value="ecommerce">E-commerce / Retail</option>
                <option value="hr_ops">HR / Operations</option>
                <option value="education">Education / EdTech</option>
                <option value="legal">Legal / LegalTech</option>
                <option value="healthcare">Healthcare / MedTech (HIPAA)</option>
                <option value="fintech">Finance / Fintech (PCI, SOC 2)</option>
              </select>
            </div>
          </div>

          <div class="sc-row">
            <div class="sc-field">
              <label>Target Revenue — Year 1 ($)</label>
              <div class="pfx">
                <span>$</span>
                <input type="number" id="fRevenue" placeholder="e.g. 120000" min="0" step="5000"/>
              </div>
              <span class="sc-err" id="errRevenue">Please enter your revenue target</span>
            </div>
            <div class="sc-field">
              <label>Your Available Budget ($)</label>
              <div class="pfx">
                <span>$</span>
                <input type="number" id="fBudget" placeholder="e.g. 50000" min="0" step="5000"/>
              </div>
              <span class="sc-err" id="errBudget">Please enter your budget</span>
            </div>
          </div>

          <div class="sc-row">
            <div class="sc-field">
              <label>Your Email</label>
              <input type="email" id="fEmail" placeholder="you@company.com"/>
              <span class="sc-err" id="errEmail">Email is required</span>
            </div>
            <div class="sc-field">
              <label>Website / Idea URL (optional)</label>
              <input type="url" id="fWebsite" placeholder="https://..."/>
            </div>
          </div>

          <button class="sc-calc-btn" id="calcBtn">Run My Free SaaS Audit &#8594;</button>

        </div><!-- /form -->

        <!-- ── RESULTS ─────────────────────────────────────────────── -->
        <div class="sc-results-panel" id="resultsPanel">
          <div class="sc-results-header">
            <h3>Your Opportunity Audit</h3>
            <span class="sc-opp-badge" id="oppBadge">&#8212;</span>
          </div>

          <div class="sc-empty" id="emptyState">
            <div class="sc-empty-icon">&#128640;</div>
            <p>Fill in your details on the left and click <strong>Run My Free SaaS Audit</strong> to see your full personalised report.</p>
          </div>

          <div id="resContent" style="display:none">

            <!-- 1. Score -->
            <div class="sc-score-section">
              <p class="sc-score-lbl">Opportunity Score</p>
              <div class="sc-score-row">
                <span class="sc-score-num" id="rScore">0</span>
                <span class="sc-score-denom">/100</span>
              </div>
              <div class="sc-prog-track">
                <div class="sc-prog-fill" id="progFill" style="width:0%"/>
              </div>
              <p class="sc-score-verdict" id="rVerdict">&#8212;</p>
              <div class="sc-score-factors" id="scoreFactors" style="display:none">
                <div class="sc-factor-row">
                  <span class="sc-factor-lbl">Budget Fit</span>
                  <div class="sc-factor-track"><div class="sc-factor-fill" id="sfBudget" style="background:#1565c0"/></div>
                  <span class="sc-factor-pts" id="sfBudgetPts">0/40</span>
                </div>
                <div class="sc-factor-row">
                  <span class="sc-factor-lbl">Market Opp.</span>
                  <div class="sc-factor-track"><div class="sc-factor-fill" id="sfMarket" style="background:#2e7d32"/></div>
                  <span class="sc-factor-pts" id="sfMarketPts">0/30</span>
                </div>
                <div class="sc-factor-row">
                  <span class="sc-factor-lbl">Audience</span>
                  <div class="sc-factor-track"><div class="sc-factor-fill" id="sfAudience" style="background:#6a1b9a"/></div>
                  <span class="sc-factor-pts" id="sfAudiencePts">0/20</span>
                </div>
                <div class="sc-factor-row">
                  <span class="sc-factor-lbl">Timeline</span>
                  <div class="sc-factor-track"><div class="sc-factor-fill" id="sfUrgency" style="background:#e65100"/></div>
                  <span class="sc-factor-pts" id="sfUrgencyPts">0/10</span>
                </div>
                <div class="sc-score-tip" id="rScoreTip"/>
              </div>
            </div>

            <!-- 2. Investment to Build It Right -->
            <div class="sc-section">
              <p class="sc-section-title"><span>&#128176;</span> Investment to Build It Right</p>
              <p class="sc-build-lbl">Estimated monthly development cost:</p>
              <div class="sc-build-amt" id="rMonthlyBuild">$0 – $0 <small style="font-size:.6em;font-weight:500;color:#888">/month</small></div>
              <div id="rBuildNotes" class="sc-build-notes" style="display:none"/>
              <div class="sc-budget-row">
                <span class="sc-budget-lbl">Total project budget estimate:</span>
                <span class="sc-budget-val" id="rTotalBudget">&#8212;</span>
              </div>
            </div>

            <!-- 3. Timeline & ROI -->
            <div class="sc-section">
              <p class="sc-section-title"><span>&#9201;</span> Timeline &amp; Return on Investment</p>
              <div class="sc-timeline-grid">
                <div class="sc-tl-card">
                  <div class="sc-tl-icon">&#128736;</div>
                  <div class="sc-tl-lbl">Estimated Build Time</div>
                  <div class="sc-tl-val" id="rBuildTime">&#8212;</div>
                </div>
                <div class="sc-tl-card">
                  <div class="sc-tl-icon">&#128200;</div>
                  <div class="sc-tl-lbl">Break-even If You Start Today</div>
                  <div class="sc-tl-val" id="rROI">&#8212;</div>
                </div>
              </div>
            </div>

            <!-- 4. Team Composition -->
            <div class="sc-section">
              <p class="sc-section-title"><span>&#128101;</span> Recommended Team Composition</p>
              <div class="sc-team-grid" id="rTeamGrid"><!-- filled by JS --></div>
              <p class="sc-team-total">Estimated team size: <strong id="rTeamSize">&#8212;</strong></p>
            </div>

            <!-- 5. Revenue at Risk -->
            <div class="sc-section">
              <p class="sc-section-title"><span>&#128293;</span> Revenue at Risk — Every Month Counts</p>
              <p class="sc-monthly-loss-lbl">Revenue you stop losing the day you launch:</p>
              <div class="sc-monthly-loss-amt" id="rMonthly">$0</div>
              <div class="sc-loss-grid">
                <div class="sc-loss-card">
                  <div class="sc-loss-period">Wait 6 More Months</div>
                  <div class="sc-loss-amount" id="rLoss6">$0</div>
                </div>
                <div class="sc-loss-card crit">
                  <div class="sc-loss-period">Wait 12 More Months</div>
                  <div class="sc-loss-amount" id="rLoss12">$0</div>
                </div>
              </div>
            </div>

            <!-- 6. Chart -->
            <div class="sc-section">
              <p class="sc-section-title"><span>&#128202;</span> Total Revenue Lost vs. Months of Delay</p>
              <div class="sc-chart-wrap">
                <canvas id="trajChart"/>
              </div>
              <p class="sc-chart-note">The red line shows total compounding revenue opportunity lost the longer you wait to launch. Hover for exact values.</p>
            </div>

            <!-- 7. Insight -->
            <div class="sc-section">
              <div class="sc-insight-card">
                <span class="sc-insight-icon">&#128161;</span>
                <p class="sc-insight-text" id="rInsight">&#8212;</p>
              </div>
            </div>

            <!-- 8. CTA -->
            <button class="sc-cta-btn" id="openAuditBtn">
              Get Full Report &amp; Book Your Free Strategy Call &#8594;
            </button>

          </div><!-- /resContent -->
        </div><!-- /results panel -->

      </div><!-- /container -->
    </div><!-- /saas-calc-root -->

    <!-- ── MODAL ────────────────────────────────────────────────────── -->
    <div class="sc-overlay" id="auditOverlay">
      <div class="sc-modal">
        <button class="sc-modal-x" id="closeModal">&#10005;</button>
        <h2>Your Full SaaS Opportunity Report</h2>
        <p class="sc-modal-sub" id="modalSub">&#8212;</p>
        <div class="sc-rec-box">
          <strong>Our Recommendation for You</strong>
          <p id="rRecommendation">&#8212;</p>
        </div>
        <div class="sc-lead-form">
          <h3>&#128236; Get Your Personalised Action Plan</h3>
          <div class="sc-lead-grid">
            <div class="sc-lf"><label>Full Name *</label><input type="text" id="lfName" placeholder="Jane Smith"/></div>
            <div class="sc-lf"><label>Phone Number</label><input type="tel" id="lfPhone" placeholder="+1 555 000 0000"/></div>
            <div class="sc-lf"><label>Company / Startup</label><input type="text" id="lfCompany" placeholder="Acme Inc."/></div>
            <div class="sc-lf"><label>Your Role</label><input type="text" id="lfRole" placeholder="Founder / CTO"/></div>
            <div class="sc-lf" style="grid-column:1/-1">
              <label>What is your biggest challenge right now?</label>
              <textarea id="lfChallenge" rows="3" placeholder="Describe your current situation&#8230;"/>
            </div>
          </div>
          <button class="sc-lead-sub" id="leadSubmit">Send Me the Action Plan &amp; Schedule a Free Call &#8594;</button>
          <div class="sc-lead-ok" id="leadOk">&#9989; Thank you! We'll review your audit and be in touch within 24 hours.</div>
        </div>
      </div>
    </div>

    <div class="sc-toast" id="toast"/>

    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"/>
    <script>
(function(){
  /* ── Data tables ── */
  var AUD={
    idea_personal:    {urgency:.85,riskMult:1.20,mktCapture:.60,insight:'Every month of delay is money from your own pocket — and an open door for competitors to enter your market first.',rec:'Start with a lean MVP targeting your first 10–20 paying customers. Budget 4–6 months of focused development with a reliable technical partner.'},
    idea_funded:      {urgency:.95,riskMult:1.70,mktCapture:.75,insight:'Investor runway has a hard deadline. Slow execution compresses your window to hit milestones and secure your next funding round.',rec:'Speed to a working product is everything. Investors expect milestones on schedule — a strong development partner can halve your time-to-demo.'},
    unsatisfied_team: {urgency:.90,riskMult:1.50,mktCapture:.65,insight:'An underperforming team doubles your cost — you pay salaries AND lose market time simultaneously.',rec:'Audit your current output vs monthly spend. Switching to the right partner typically saves 40–60% long-term and recovers 3–6 months of velocity.'},
    mvp_investment:   {urgency:.80,riskMult:1.40,mktCapture:.70,insight:'Your MVP proves the concept. Now you need execution speed and code quality to scale before competitors replicate your idea.',rec:'Prioritise scalability, code quality, and feature delivery speed. The right team pays for itself within 3–4 months.'},
    mvp_ai:           {urgency:.88,riskMult:1.60,mktCapture:.60,insight:'AI-generated code carries hidden technical debt — security vulnerabilities, no scalability plan, logic that breaks under real loads.',rec:'Get a professional technical audit before scaling marketing or pitching investors. Fixing AI-built MVPs early costs 3x less.'},
    non_technical:    {urgency:.75,riskMult:1.10,mktCapture:.50,insight:'Without technical knowledge, the wrong development vendor can burn your entire budget with little to show.',rec:'Start with a discovery and prototype phase before committing to full development. Validate cheaply, then scale with confidence.'},
  };
  var ST={
    b2b:        {competition:.60,baseMonths:5},
    b2c:        {competition:.85,baseMonths:6},
    marketplace:{competition:.78,baseMonths:9},
    enterprise: {competition:.50,baseMonths:14},
  };
  var CMP={
    mvp:       {monthlyMin:8000,  monthlyMax:15000, timeMult:.70},
    standard:  {monthlyMin:15000, monthlyMax:28000, timeMult:1.00},
    complex:   {monthlyMin:28000, monthlyMax:50000, timeMult:1.45},
    enterprise:{monthlyMin:45000, monthlyMax:90000, timeMult:1.85},
  };
  var USR={small:{sf:1.00},medium:{sf:1.20},large:{sf:1.55},xlarge:{sf:1.95}};
  var TL={asap:{um:1.50,cp:1.30},short:{um:1.20,cp:1.10},medium:{um:1.00,cp:1.00},long:{um:0.80,cp:0.90}};
  var AI={
    no_ai:    {cm:1.00,tm:1.00,note:''},
    ai_api:   {cm:1.20,tm:1.15,note:'AI API integration adds prompt engineering, reliability handling and ongoing API cost.'},
    ai_custom:{cm:1.65,tm:1.50,note:'Custom ML requires data pipelines, model training, evaluation and MLOps infrastructure.'},
    ai_core:  {cm:2.00,tm:1.80,note:'AI-first products need significant R&amp;D, data engineering and specialist AI/ML talent.'},
  };
  var IND={
    general:   {cm:1.00,tm:1.00,note:''},
    ecommerce: {cm:1.05,tm:1.05,note:''},
    hr_ops:    {cm:1.10,tm:1.08,note:''},
    education: {cm:1.10,tm:1.08,note:''},
    legal:     {cm:1.30,tm:1.25,note:'Legal domain complexity and data sensitivity add meaningful scope.'},
    healthcare:{cm:1.50,tm:1.45,note:'HIPAA compliance, audit trails and security certification add ~45% to cost and timeline.'},
    fintech:   {cm:1.55,tm:1.50,note:'PCI DSS and SOC 2 require security audits, penetration testing and compliance work.'},
  };

  /* ── Helpers ── */
  var fmt  = function(n){return '$'+Math.round(n).toLocaleString();};
  var fmtK = function(n){var v=Math.round(n);return v>=1000?'$'+(v/1000).toFixed(0)+'K':fmt(v);};
  var flt  = function(id){return parseFloat(document.getElementById(id).value)||0;};
  var sel  = function(id){return document.getElementById(id).value;};

  function monthlyBuild(ck,uk,tk,aik,ik){var c=CMP[ck],u=USR[uk],t=TL[tk],ai=AI[aik]||AI.no_ai,ind=IND[ik]||IND.general;return{min:Math.round(c.monthlyMin*u.sf*t.cp*ai.cm*ind.cm),max:Math.round(c.monthlyMax*u.sf*t.cp*ai.cm*ind.cm)};}
  function buildTime(sk,ck,budget,aik,ik){var base=ST[sk].baseMonths;var sp=budget>=200000?.80:budget>=100000?.90:budget>=50000?1.00:1.25;var ai=AI[aik]||AI.no_ai,ind=IND[ik]||IND.general;return Math.min(36,Math.max(2,Math.round(base*CMP[ck].timeMult*sp*ai.tm*ind.tm)));}
  function oppScore(ak,sk,ck,uk,tk,aik,ik,budget,rev){
    var a=AUD[ak],s=ST[sk],t=TL[tk],mb=monthlyBuild(ck,uk,tk,aik,ik),bt=buildTime(sk,ck,budget,aik,ik);
    var fit=Math.min(budget/Math.max(mb.min*bt,1),1),mkt=1-s.competition,urg=Math.min(t.um/1.5,1);
    return Math.round(Math.min(99,Math.max(5,(fit*40)+(mkt*30)+(a.mktCapture*20)+(urg*10))));
  }
  function monthlyLost(rev,ak,sk,tk){return Math.round((rev/12)*AUD[ak].urgency*ST[sk].competition*TL[tk].um);}
  function delayLoss(mo,months,mult){var t=0;for(var i=0;i&lt;months;i++)t+=mo*Math.pow(1.05,i);return Math.round(t*mult);}
  function verdict(sc){if(sc>=80)return{text:'Strong Opportunity',cls:'opp-strong'};if(sc>=60)return{text:'Good Opportunity',cls:'opp-good'};if(sc>=40)return{text:'Moderate — Act Strategically',cls:'opp-moderate'};if(sc>=20)return{text:'Challenging — Plan Carefully',cls:'opp-challenging'};return{text:'High Risk — Seek Guidance',cls:'opp-risky'};}

  var TEAM_ROLES={
    designer:{icon:'🎨',label:'UI/UX Designer',    desc:'Branding, wireframes, user flows &amp; visual design'},
    frontend:{icon:'💻',label:'Frontend Engineer',  desc:'React/Vue, responsive UI &amp; performance'},
    backend: {icon:'⚙️',label:'Backend Engineer',   desc:'APIs, database, business logic &amp; integrations'},
    sqa:     {icon:'🧪',label:'QA / Test Engineer', desc:'Manual &amp; automated testing, release sign-off'},
    pm:      {icon:'📋',label:'Project Manager',    desc:'Sprint planning, delivery &amp; stakeholder comms'},
    devops:  {icon:'🔧',label:'DevOps Engineer',    desc:'CI/CD pipelines, cloud infrastructure &amp; monitoring'},
    aiml:    {icon:'🤖',label:'AI / ML Engineer',   desc:'Model training, data pipelines &amp; MLOps'},
    analyst: {icon:'📊',label:'Business Analyst',   desc:'Requirements, domain research &amp; user stories'},
    support: {icon:'🛟',label:'Support Engineer',   desc:'Post-launch support, incidents &amp; onboarding'},
  };
  function calcTeam(ck,aik,ik,uk){
    var t={designer:{min:1,max:1},frontend:{min:1,max:2},backend:{min:1,max:2},sqa:{min:1,max:1},pm:{min:1,max:1},devops:{min:0,max:1},aiml:{min:0,max:0},analyst:{min:0,max:1},support:{min:0,max:0}};
    if(ck==='standard') {t.backend.max=3;}
    if(ck==='complex')  {t.frontend.min=2;t.frontend.max=3;t.backend.min=2;t.backend.max=4;t.devops.min=1;t.analyst.min=1;}
    if(ck==='enterprise'){t.frontend.min=2;t.frontend.max=4;t.backend.min=3;t.backend.max=6;t.sqa.max=2;t.pm.max=2;t.devops.min=1;t.analyst.min=1;t.support.min=1;t.support.max=1;}
    if(aik==='ai_api')   {t.aiml.max=1;}
    if(aik==='ai_custom'){t.aiml.min=1;t.aiml.max=2;}
    if(aik==='ai_core')  {t.aiml.min=2;t.aiml.max=3;t.backend.min=Math.max(t.backend.min,2);}
    if(ik==='healthcare'||ik==='fintech'||ik==='legal'){t.sqa.min=1;t.analyst.min=1;}
    if(uk==='large'||uk==='xlarge'){t.devops.min=1;t.support.min=1;t.support.max=1;}
    var res=[];for(var k in t){if(t[k].max>0)res.push({key:k,min:t[k].min,max:t[k].max});}
    return res;
  }

  var fmtAxis=function(v){if(v>=1000000)return'$'+(v/1000000).toFixed(1)+'M';if(v>=1000)return'$'+(v/1000).toFixed(0)+'K';return'$'+v;};
  var trajCh=null;
  function drawTraj(data){
    var canvas=document.getElementById('trajChart');
    if(trajCh){trajCh.destroy();trajCh=null;}
    trajCh=new Chart(canvas,{
      type:'line',
      data:{
        labels:data.map(function(_,i){return i===0?'Now':i+' mo';}),
        datasets:[{
          label:'Revenue Lost',data:data,borderColor:'#e53935',backgroundColor:'rgba(229,57,53,.07)',
          borderWidth:2.5,pointRadius:data.map(function(_,i){return i%3===0?3:0;}),
          pointBackgroundColor:'#e53935',fill:true,tension:.4,
        }]
      },
      options:{
        responsive:true,maintainAspectRatio:false,
        plugins:{
          legend:{display:false},
          tooltip:{enabled:true,callbacks:{title:function(items){return'Delay: '+items[0].label;},label:function(item){return'  Revenue lost: '+fmt(item.raw);}},backgroundColor:'#1a237e',titleColor:'#fff',bodyColor:'#fff',padding:8,cornerRadius:6},
        },
        scales:{
          x:{display:true,title:{display:true,text:'Months of delay',font:{size:9},color:'#999'},ticks:{font:{size:9},color:'#aaa',maxTicksLimit:7},grid:{display:false},border:{color:'#e8e8e8'}},
          y:{display:true,title:{display:true,text:'Cumulative revenue lost',font:{size:9},color:'#999'},ticks:{font:{size:9},color:'#aaa',callback:fmtAxis,maxTicksLimit:5},grid:{color:'rgba(0,0,0,.05)'},border:{color:'#e8e8e8'}},
        },
        animation:{duration:900},
      },
    });
  }

  function validate(){
    var ok=true,em=document.getElementById('fEmail').value.trim(),rv=flt('fRevenue'),bud=flt('fBudget');
    var show=function(id,v){var el=document.getElementById(id);if(v){el.classList.add('show');ok=false;}else el.classList.remove('show');};
    if(!em||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(em)){document.getElementById('errEmail').textContent=em?'Invalid email':'Email is required';show('errEmail',true);}else show('errEmail',false);
    show('errRevenue',rv&lt;=0); show('errBudget',bud&lt;=0);
    return ok;
  }

  document.getElementById('calcBtn').addEventListener('click',function(){
    if(!validate())return;
    var ak=sel('fAudience'),sk=sel('fSaasType'),ck=sel('fComplexity'),uk=sel('fUsers'),tk=sel('fTimeline');
    var aik=sel('fAI'),ik=sel('fIndustry');
    var rev=flt('fRevenue'),budget=flt('fBudget');
    var a=AUD[ak];

    var mb   = monthlyBuild(ck,uk,tk,aik,ik);
    var bt   = buildTime(sk,ck,budget,aik,ik);
    var sc   = oppScore(ak,sk,ck,uk,tk,aik,ik,budget,rev);
    var mo   = monthlyLost(rev,ak,sk,tk);
    var l6   = delayLoss(mo,6,a.riskMult);
    var l12  = delayLoss(mo,12,a.riskMult);
    var tbMin= mb.min*bt, tbMax=mb.max*bt;
    var roi  = rev>0?Math.ceil(tbMax/(rev/12)):null;
    var verd = verdict(sc);
    var traj = [];for(var i=0;i&lt;=12;i++)traj.push(i===0?0:delayLoss(mo,i,a.riskMult));

    /* Badge */
    var badge=document.getElementById('oppBadge');
    badge.textContent=verd.text; badge.className='sc-opp-badge '+verd.cls;

    /* Score */
    document.getElementById('rScore').textContent=sc;
    document.getElementById('rVerdict').textContent=verd.text;
    var col=sc>=80?'#2e7d32':sc>=60?'#1565c0':sc>=40?'#ef6c00':'#c62828';
    var fill=document.getElementById('progFill');
    fill.style.background=col; setTimeout(function(){fill.style.width=sc+'%';},50);

    /* Score factor breakdown */
    var minTotal=mb.min*bt;
    var budgetFit=Math.min(budget/Math.max(minTotal,1),1);
    var marketFactor=1-ST[sk].competition;
    var urgencyFactor=Math.min(TL[tk].um/1.5,1);
    var mktCap=a.mktCapture;
    document.getElementById('sfBudgetPts').textContent=Math.round(budgetFit*40)+'/40';
    document.getElementById('sfMarketPts').textContent=Math.round(marketFactor*30)+'/30';
    document.getElementById('sfAudiencePts').textContent=Math.round(mktCap*20)+'/20';
    document.getElementById('sfUrgencyPts').textContent=Math.round(urgencyFactor*10)+'/10';
    setTimeout(function(){
      document.getElementById('sfBudget').style.width=Math.round(budgetFit*100)+'%';
      document.getElementById('sfMarket').style.width=Math.round(marketFactor*100)+'%';
      document.getElementById('sfAudience').style.width=Math.round(mktCap*100)+'%';
      document.getElementById('sfUrgency').style.width=Math.round(urgencyFactor*100)+'%';
    },60);
    var tipPct=Math.round(budgetFit*100);
    var scoreTipTxt;
    if(budgetFit&lt;0.40)scoreTipTxt='&#9888;&#65039; Budget covers only '+tipPct+'% of the min build cost ('+fmt(minTotal)+'). Increasing budget or simplifying scope would have the biggest impact on your score.';
    else if(budgetFit&lt;0.75)scoreTipTxt='&#128161; Budget covers '+tipPct+'% of the estimated build cost — closing this gap would add up to '+Math.round((1-budgetFit)*40)+' more points.';
    else if(marketFactor&lt;0.25)scoreTipTxt='&#9889; Highly competitive market. Fast execution and strong differentiation are critical to claiming your opportunity window.';
    else if(urgencyFactor&lt;0.85)scoreTipTxt='&#128197; ASAP timeline carries a 30% cost premium. A 3–6 month plan reduces risk and lifts your score.';
    else scoreTipTxt='&#9989; Your opportunity is well-balanced. Focus on shipping fast and signing your first paying customers.';
    document.getElementById('rScoreTip').textContent=scoreTipTxt;
    document.getElementById('scoreFactors').style.display='block';

    /* Cost of doing nothing */
    document.getElementById('rMonthly').textContent=fmt(mo);
    document.getElementById('rLoss6').textContent=fmt(l6);
    document.getElementById('rLoss12').textContent=fmt(l12);

    /* Investment */
    document.getElementById('rMonthlyBuild').innerHTML=fmtK(mb.min)+' – '+fmtK(mb.max)+'<small style="font-size:.6em;font-weight:500;color:#888"> /month</small>';
    document.getElementById('rTotalBudget').textContent=fmtK(tbMin)+' – '+fmtK(tbMax);
    var notes=[];
    if(AI[aik]&amp;&amp;AI[aik].note)notes.push(AI[aik].note);
    if(IND[ik]&amp;&amp;IND[ik].note)notes.push(IND[ik].note);
    var noteEl=document.getElementById('rBuildNotes');
    noteEl.innerHTML=notes.map(function(n){return'&lt;span class="sc-build-note"&gt;&#9881; '+n+'&lt;/span&gt;';}).join('');
    noteEl.style.display=notes.length?'block':'none';

    /* Timeline */
    document.getElementById('rBuildTime').textContent=bt+(bt===1?' month':' months');
    document.getElementById('rROI').textContent=roi?roi+(roi===1?' month':' months'):'N/A';

    /* Chart */
    setTimeout(function(){drawTraj(traj);},100);

    /* Team composition */
    var team=calcTeam(ck,aik,ik,uk);
    var tMin=team.reduce(function(s,r){return s+r.min;},0);
    var tMax=team.reduce(function(s,r){return s+r.max;},0);
    document.getElementById('rTeamSize').textContent=tMin+'–'+tMax+' people';
    document.getElementById('rTeamGrid').innerHTML=team.map(function(r){
      var ro=TEAM_ROLES[r.key];
      var cnt=r.min===r.max?r.min+'×':(r.min+' – '+r.max+'×');
      return'&lt;div class="sc-team-card"&gt;&lt;div class="sc-team-card-icon"&gt;'+ro.icon+'&lt;/div&gt;&lt;div class="sc-team-card-role"&gt;'+ro.label+'&lt;/div&gt;&lt;div class="sc-team-card-count"&gt;'+cnt+'&lt;/div&gt;&lt;div class="sc-team-card-desc"&gt;'+ro.desc+'&lt;/div&gt;&lt;/div&gt;';
    }).join('');

    /* Insight */
    document.getElementById('rInsight').textContent=a.insight;

    /* Show */
    document.getElementById('emptyState').style.display='none';
    document.getElementById('resContent').style.display='block';

    window._audit={ak:ak,sk:sk,ck:ck,uk:uk,tk:tk,aik:aik,ik:ik,rev:rev,budget:budget,sc:sc,mo:mo,l6:l6,l12:l12,mb:mb,tbMin:tbMin,tbMax:tbMax,bt:bt,roi:roi,verd:verd,a:a,tMin:tMin,tMax:tMax};
  });

  document.getElementById('openAuditBtn').addEventListener('click',function(){
    if(!window._audit)return;
    var d=window._audit;
    document.getElementById('modalSub').textContent='Score '+d.sc+'/100 · '+d.verd.text+' · Monthly loss if delayed: '+fmt(d.mo)+' · 12-month risk: '+fmt(d.l12);
    document.getElementById('rRecommendation').textContent=d.a.rec;
    document.getElementById('leadOk').style.display='none';
    document.getElementById('leadSubmit').style.display='block';
    document.getElementById('auditOverlay').classList.add('on');
  });

  document.getElementById('closeModal').addEventListener('click',function(){document.getElementById('auditOverlay').classList.remove('on');});
  document.getElementById('auditOverlay').addEventListener('click',function(e){if(e.target===document.getElementById('auditOverlay'))document.getElementById('auditOverlay').classList.remove('on');});
  document.addEventListener('keydown',function(e){if(e.key==='Escape')document.getElementById('auditOverlay').classList.remove('on');});

  document.getElementById('leadSubmit').addEventListener('click',function(){
    var name=document.getElementById('lfName').value.trim();
    if(!name){alert('Please enter your name.');return;}
    var d=window._audit||{};
    var payload={
      name:name,phone:document.getElementById('lfPhone').value.trim(),
      company:document.getElementById('lfCompany').value.trim(),role:document.getElementById('lfRole').value.trim(),
      challenge:document.getElementById('lfChallenge').value.trim(),
      email:document.getElementById('fEmail').value.trim(),
      website:document.getElementById('fWebsite').value.trim(),
      audienceType:d.ak,saasType:d.sk,complexityKey:d.ck,usersKey:d.uk,timelineKey:d.tk,aiKey:d.aik,industryKey:d.ik,
      revenueGoal:d.rev,budget:d.budget,
      auditResults:{
        opportunityScore:d.sc,monthlyRevLost:d.mo,loss6Months:d.l6,loss12Months:d.l12,
        monthlyBuildMin:d.mb?d.mb.min:0,monthlyBuildMax:d.mb?d.mb.max:0,
        totalBudgetMin:d.tbMin,totalBudgetMax:d.tbMax,
        buildTimeline:d.bt,roiMonths:d.roi,verdict:d.verd?d.verd.text:'',teamSize:(d.tMin||0)+'–'+(d.tMax||0)+' people',
      },
      source:'saas_audit_calculator',
    };
    fetch('/saas-calculator/submit-lead',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}).catch(function(){});
    document.getElementById('leadSubmit').style.display='none';
    document.getElementById('leadOk').style.display='block';
    var t=document.getElementById('toast');t.textContent='Thank you! We will be in touch within 24 hours.';t.classList.add('on');setTimeout(function(){t.classList.remove('on');},4500);
  });
})();
    </script>

  </template><!-- /calculator_embed -->


  <!-- ═══════════════════════════════════════════════════════════════════════
       STANDALONE PAGE  — served at  /saas-calculator
       ═══════════════════════════════════════════════════════════════════════ -->
  <template id="calculator_page" name="SaaS Opportunity Audit Calculator">
    <t t-call="website.layout">
      <t t-set="additional_title">Free SaaS Opportunity Audit</t>
      <t t-set="meta_description">Find out how much revenue you're leaving on the table and what it costs to delay. Free SaaS product audit in 60 seconds.</t>

      <!-- Make Odoo header/footer match the dark blue theme -->
      <style>
        .o_header_standard { background:#0b1731 !important; }
        .o_footer { background:#0b1731 !important; }
        #wrapwrap { background:linear-gradient(135deg,#0b1731 0%,#1a237e 45%,#1565c0 100%) !important; }
      </style>

      <t t-call="saas_audit_calculator.calculator_embed"/>
    </t>
  </template>


  <!-- ═══════════════════════════════════════════════════════════════════════
       WEBSITE SNIPPET  — draggable building block for the website editor
       ═══════════════════════════════════════════════════════════════════════ -->
  <template id="s_saas_calculator" name="SaaS Audit Calculator">
    <section class="s_saas_calc o_colored_level">
      <t t-call="saas_audit_calculator.calculator_embed"/>
    </section>
  </template>

  <!-- Register the snippet in the website editor sidebar -->
  <template id="saas_audit_snippets" inherit_id="website.snippets" name="SaaS Audit — Snippet">
    <xpath expr="//div[@id='snippet_structure']" position="before">
      <div class="o_panel">
        <div class="o_panel_header">&#128640; SaaS Tools</div>
        <div class="o_panel_body">
          <div t-snippet="saas_audit_calculator.s_saas_calculator"
               t-thumbnail="/saas_audit_calculator/static/description/icon.png">
            <span>SaaS Audit Calculator</span>
          </div>
        </div>
      </div>
    </xpath>
  </template>

</odoo>
